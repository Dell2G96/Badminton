# Repository Guidelines

## 작업 언어 및 문서 저장 규칙

- 모든 설명, 계획, 분석, 설계, 질문과 결정 사항, 작업 내역, 검증 결과 및 최종 답변은 한국어를 기본으로 작성하고 Markdown(`.md`) 파일로 남긴다. 코드 식별자, 명령어와 공식 명칭은 원문을 유지할 수 있다.
- 문서 저장 루트는 `D:\_Projects\FPS_Dell2g\Docs`다. 작업한 날짜를 한국 시간(`Asia/Seoul`) 기준으로 확인하고 `YY.MM.DD` 형식의 폴더를 생성한다. 예: `Docs/26.09.14/`.
- 문서 파일명은 `<작업날짜> - 작업 제목 - <작업 모델명>.md` 형식을 사용한다. 작업날짜도 `YY.MM.DD`로 통일한다. 예: `26.09.14 - 배드민턴 GAS 초기 개발계획서 - GPT-6.md`.
- 작업 모델명에는 실행 환경에서 확인 가능한 실제 모델명을 사용한다. 정확한 세부 모델명을 알 수 없으면 확인 가능한 모델명까지만 쓰고, 전혀 확인할 수 없으면 `모델명 미확인`으로 기록한다. 임의의 모델명이나 버전을 추측하지 않는다.
- 파일명에 사용할 수 없는 문자(`\ / : * ? " < > |`)는 제거하거나 안전한 문자로 바꾼다. 같은 날 같은 제목의 파일이 이미 있으면 관련 작업은 이어서 갱신하고, 별개 작업이면 작업 제목에 구분을 추가한다. 다른 작업의 내용을 덮어쓰지 않는다.
- 매 작업의 실질적인 내용을 날짜별 문서에 기록한다. 진행 답변은 작업 기록에 누적하고, 질문과 사용자 결정도 기록하며, 최종 답변은 보내기 전에 문서에 반영한다. 채팅에만 내용을 남기지 않는다. 최종 답변에는 작성·수정한 문서 링크와 핵심 결과를 제공한다.
- 작업 기록에는 요청 요약, 수행 내용, 변경 파일, 검증 결과, 미완료 사항을 포함한다. 수행하지 않은 구현이나 테스트를 완료로 표시하지 않는다. 비밀 값과 개인 인증 정보는 기록하지 않는다.
- 날짜가 바뀌면 해당 날짜의 폴더에 후속 문서를 작성하고 이전 기록을 연결한다. 기존 날짜의 기록을 새 날짜로 이동하지 않는다.
- 이 규칙은 문서 및 답변 산출물에 적용한다. 소스·에셋·프로젝트 설정은 각 정상 경로와 확장자를 유지하며, 저장소 지침 파일인 `AGENTS.md`는 루트에 유지한다. 실제 개발계획서와 상세 작업 기록은 `Docs`에 둔다.

## 프로젝트 목표와 현재 구현 상태

현재 저장소는 Unreal Engine 5.7 기반 C++ FPS 프로젝트이며, 신규 개발 목표는 GAS를 활용하는 배드민턴 멀티플레이 게임이다. 기존 FPS 기반 코드와 배드민턴 목표를 구분하여 설명한다. 프로젝트명과 런타임 모듈명 `FPS_Dell2g`는 별도 요청이 없으면 유지한다.

2026-09-14 구현 작업에서 `GameplayAbilities`, `GameplayTags`, `GameplayTasks` 모듈과 GameplayAbilities 플러그인을 활성화했다. 배드민턴 전용 클래스는 `Source/FPS_Dell2g/Badminton/`, 새 기본 맵은 `/Game/Badminton/Maps/L_Badminton_Prototype`에 있다. ASC·Stamina 초기화, 2인 입장·이동·준비, GAS 기본 타격과 서버 셔틀콕 비행을 구현했다. 후속 작업에서 서브·드롭·스매시·예측 대시, 스태미나 비용·회복, 서버의 11점 경기·서브권·종료·재경기, 결과 HUD와 샷 DataAsset을 구현했다. 독립 프로세스의 비용·이동·랠리·경기 종료·재경기 및 규칙/궤적 자동화 검증을 수행했다. Windows Development 패키징과 해당 실행 파일의 2인 랠리·경기·능력·재접속 검사도 통과했다. 패키지는 Saved/Packages/BadmintonMVP/Windows, 실행 메뉴는 Start-BadmintonPackage.cmd다. 실제 EOS 인증·외부망, 사람의 플레이테스트와 단계별 MP4 영상은 별도 미완료 사항이다. 구체적인 조건과 한계는 날짜별 사용자 부재 중 단계별 구현 기록을 확인한다.

사용자 결정에 따라 기존 OBSRecording 플러그인은 `.uproject`에서 임시 비활성화했다. 관련 소스는 보존한다. 2026-09-14 EOS 조직 `KOREA`, 제품 `Lee`, Live Deployment와 `BadmintonGameClient`/`BadmintonPeer2Peer` 생성 및 Epic Account Services 권한·클라이언트 연결을 사용자 화면에서 확인했다. 브랜드 설정은 도메인 부재로 보류했다. EOS 로그인과 2인 로비 코드를 추가했고 빌드 및 합성 설정 테스트는 통과했지만 실제 계정 로그인과 외부망 접속은 미검증이다. `Start-BadmintonEOS.cmd`를 사용자 Windows 환경에서 실행해 검증한다.

초기 개발계획서는 [배드민턴 GAS 초기 개발계획서](<Docs/26.09.14/26.09.14 - 배드민턴 GAS 초기 개발계획서 - GPT-6.md>)를 기준으로 한다. 범위나 설계가 바뀌면 작업일 폴더에 변경 이유와 갱신한 계획을 기록한다.

## 배드민턴 MVP 및 개발 순서

- Windows PC, 1대1 단식, 코트 1개, 동일 능력치 캐릭터를 초기 범위로 한다. 높은 3인칭 카메라와 키보드·마우스 조작을 우선한다.
- 첫 완성 목표는 두 플레이어가 접속하여 서브, 랠리, 득점, 경기 종료, 재경기까지 진행하는 빌드다. Listen Server와 LAN/직접 IP 접속부터 검증한다. 인터넷 방 검색이나 NAT 통과까지 구현된 것으로 설명하지 않는다.
- 사용자 확정 요구사항: 첫 배포에 EOS 기반 인터넷 방 생성·검색·참가를 포함한다. EOS는 후순위 선택 기능이 아닌 배포 완료 조건이다. 로컬 IP 접속 검증과 EOS 로그인·세션·P2P·외부망 검증을 구분한다.
- 기본 이동, 서브, 기본 리턴/클리어, 드롭, 스매시, 대시, 스태미나, 점수·결과 UI를 MVP 대상으로 한다. 기본 타격은 스태미나 부족으로 차단하지 않는다.
- 초기 경기 규칙은 랠리당 1점, 11점 선취 단판, 듀스 없는 자체 간소화 규칙이다. 공식 규칙의 완전 구현으로 표시하지 않는다. 서브 구역, 인·아웃, 연속 타격과 네트 통과 결과를 서버에서 판정한다. 네트 접촉만으로 즉시 실점시키지 않는다.
- 2대2 복식, AI, 자동 매칭, 전용 서버 배포, 랭크, 관전·리플레이, 성장·장비·꾸미기는 후속 범위다. EOS 방 생성·검색·참가는 첫 배포 범위에 포함한다.
- 개발 순서는 `2인 접속·이동 → GAS 기본 타격 → 셔틀콕 복제·지연 검증 → 샷 확장 → 경기 규칙 → UI·연출 → 패키징·플레이테스트`로 한다.
- 1인 개발 약 10주는 초기 추정치이며 확정 일정이 아니다. GAS 학습 기간은 별도로 산정한다. 기본 랠리의 동기화가 불안정하면 콘텐츠 추가보다 판정과 복제를 먼저 개선한다.

## GAS 및 멀티플레이 설계 원칙

- GAS는 타격·대시 능력, 스태미나, 비용·회복·재사용 제한, 행동 상태, 연출을 담당한다. 셔틀콕 시뮬레이션과 경기 규칙은 별도 게임 로직으로 구현한다.
- ASC와 AttributeSet은 PlayerState에 두고 Character를 Avatar로 연결하는 설계를 기본안으로 한다. 서버의 소유권 설정과 클라이언트 PlayerState 복제 시점에 초기화하며, 능력은 서버에서 부여하고 중복 부여를 방지한다.
- 기본 능력은 `GA_Serve`, `GA_Clear`, `GA_Drop`, `GA_Smash`, `GA_Dash`다. 공통 샷 검증을 공유하고 궤적·발사 범위·타격 구간을 DataAsset으로 관리한다.
- `State.Serving`, `State.Swinging`, `State.Recovering` 등의 태그로 행동을 제한한다. 능력 종료·취소·경기 종료 시 태그, 대기 작업과 델리게이트를 정리한다.
- 기본 이동은 CharacterMovementComponent를 사용하고 대시는 이동 예측과 호환되게 구현한다. 직접적인 클라이언트 위치 변경에 의존하지 않는다.
- Local Predicted Ability로 입력 반응을 개선하되, GAS가 셔틀콕 궤적 예측이나 타격 지연 보상을 자동으로 해결한다고 가정하지 않는다.
- 타격 성공, 셔틀콕 궤적·충돌, 점수·승패·서브권은 서버가 확정한다. 클라이언트의 타격 성공 주장, 셔틀콕 위치나 임의의 과거 입력 시각을 신뢰하지 않는다.
- 타격 시 서버가 거리·높이·방향·유효 시간·행동 상태를 검증한다. 라켓 메시 충돌이나 클라이언트 애니메이션 Notify에만 의존하지 않고, 서버 시간과 샷 데이터의 타격 구간을 사용한다.
- 셔틀콕은 서버의 전용 이동 컴포넌트에서 중력·항력을 계산하고 Sweep과 제한된 서브스텝으로 빠른 이동의 충돌 누락을 줄인다. 클라이언트는 서버 시간 기반 보간과 제한적 외삽으로 표시한다.
- 셔틀 상태에 랠리 ID와 샷 시퀀스를 포함하여 중복·오래된 요청을 거절한다. 한 스윙당 한 번만 타격을 소비하고 득점은 랠리당 한 번만 확정한다.
- 지연 보상은 오차를 계측한 뒤 필요한 범위로 추가한다. 과거 타격을 승인한다면 현재까지 궤적을 다시 계산하며 이미 확정된 랠리와 충돌하는 요청을 차단한다.
- 비용은 능력 발동 승인 시 한 번만 차감한다. 헛스윙 비용은 유지하고 발동 거절 시 예측 비용을 복구한다. 예측·확정 연출의 중복도 방지한다.
- GameMode는 서버 경기 규칙, GameState는 점수·경기 단계·서브권 복제, PlayerState는 플레이어 식별·GAS를 담당한다. GAS 태그가 별도의 경기 상태 원본이 되지 않도록 한다.
- C++은 권한 검증·복제·이동·공통 기반을, Blueprint는 외형·애니메이션·UI·연출과 데이터 조정을 중심으로 담당한다.

## 프로젝트 구조 및 모듈 구성

런타임 모듈은 `Source/FPS_Dell2g/`에 있으며, 기존 캐릭터 및 컨트롤러 코드는 `Character/`, 게임 규칙은 `Game/`에 둔다. 배드민턴 기능은 기존 구조와 참조를 확인한 뒤 역할에 맞게 추가한다. 모듈 의존성은 `Source/FPS_Dell2g/FPS_Dell2g.Build.cs`, 게임·에디터 빌드 대상은 `Source/*.Target.cs`에서 관리한다. 프로젝트 설정은 `Config/`, 에셋과 블루프린트는 `Content/`, 문서는 `Docs/YY.MM.DD/`에 저장한다. `Binaries/`, `Intermediate/`, `DerivedDataCache/`, `Saved/`, `.vs/`는 생성 산출물이므로 직접 수정하거나 커밋하지 않는다.

## 빌드, 테스트 및 로컬 실행
- 사용자 작업 방식 선호(2026-09-15): computer-use 사용을 최소화한다. 코드·설정·문서 수정, 빌드/패키징, 자동 테스트 실행, 로그와 파일 확인, 저장된 영상의 디코딩 검증은 가능한 한 CLI·기존 스크립트·전용 API로 수행한다. 실제 화면의 클릭/키 입력이 반드시 필요한 경우에만 UI 자동화를 검토한다.
- Windows + Alt + R 녹화는 사용자가 시작/종료 단축키를 직접 누르는 방식으로 협업한다. 자동 테스트는 스크립트로 실행하고 저장된 녹화 결과를 파일로 검증한다. 도구가 거절한 단축키 입력을 다른 키 주입 방식이나 간접 실행으로 우회하지 않는다. 수동 녹화와 테스트의 순서를 맞추고 녹화 중인 게임 프로세스를 임의로 재시작하지 않는다.

아래 명령은 Unreal Engine 설치 폴더의 `Engine/Build/BatchFiles`에서 실행하거나 전체 경로로 호출한다.

- `GenerateProjectFiles.bat <repo>\FPS_Dell2g.uproject`: Visual Studio 솔루션을 다시 생성한다.
- `Build.bat FPS_Dell2gEditor Win64 Development <repo>\FPS_Dell2g.uproject -WaitMutex`: 에디터 대상을 컴파일한다.
- `UnrealEditor.exe <repo>\FPS_Dell2g.uproject`: 프로젝트를 에디터에서 실행한다.
- Visual Studio에서는 `FPS_Dell2g.sln`을 열고 `Development Editor | Win64` 구성을 빌드한다.

## 코딩 스타일 및 명명 규칙

기존 코드와 Unreal Engine C++ 규칙을 따른다. 들여쓰기는 탭을 사용하고 중괄호는 새 줄에 둔다. 타입과 함수는 PascalCase, 불리언은 `b` 접두사를 사용한다. UObject 계열에는 표준 접두사(`A`, `U`, `F`, `E`, `I`)를 유지한다. 리플렉션 대상 멤버에는 `UPROPERTY`/`UFUNCTION`을 선언하고, 포인터 소유권에는 가능한 한 `TObjectPtr`를 사용한다. 헤더는 최소화하고 해당 클래스 헤더를 `.cpp`의 첫 include로 둔다.

## 테스트 지침

자동화 테스트 유무는 작업 시 확인한다. 필요한 새 테스트는 `Source/FPS_Dell2g/Tests/`에 Unreal Automation Test로 추가하고 `FPS_Dell2g.<기능>.<동작>` 형식으로 이름을 지정한다. 코드 변경 시 에디터 빌드 및 관련 맵의 PIE 동작을 확인하고, 자동화 테스트가 추가되면 `UnrealEditor-Cmd.exe ... -ExecCmds="Automation RunTests FPS_Dell2g;Quit" -unattended -nop4`로 실행한다. 명령의 `...`는 실제 프로젝트 경로와 필요한 인수로 대체한다.

- 점수 전환, 서비스 구역, 오래된 요청 거절, 중복 타격처럼 결과가 명확한 핵심 로직을 우선 검증한다.
- 멀티플레이 변경은 호스트·클라이언트를 별도 프로세스로 실행하고, 배포 전 패키징 빌드를 별도 PC/LAN에서도 검증한다.
- 네트워크 테스트는 RTT 0/50/100/150ms와 패킷 손실 0/1/3%를 기준으로 한다. 편도 지연 설정과 실제 RTT를 구분하고 측정값을 기록한다.
- 초기 목표는 RTT 100ms·손실 1%에서 반복 랠리가 가능하고, 경기 종료 후 두 화면의 점수·서브권이 일치하며 중복 득점이 없는 것이다. 150ms·3%는 스트레스 조건으로 사용한다.
- 늦은 타격 요청, 스태미나 부족, 연속 입력, 네트 걸림, 재경기, 참가자 이탈과 호스트 종료를 확인한다. 호스트와 참가자 역할을 바꿔 편향도 점검한다.
- 문서만 변경한 작업은 내용·파일명·날짜 폴더·링크·diff 검증으로 충분하며 에디터 빌드를 요구하지 않는다. 실행할 수 없는 검증은 이유와 미검증 범위를 작업 문서에 기록한다.

## 단계별 테스트 시연 영상 기록
- 사용자 지정 녹화 방식(2026-09-15): 앞으로 테스트 플레이 영상은 사용자가 지정한 Windows + Alt + R 녹화 기능을 기본으로 사용한다. 다른 녹화 프로그램으로 임의 변경하지 않는다. 실제 녹화 시작/중지 및 저장 파일을 확인하고, 영상을 디코딩해 검은 화면이 아닌 실제 게임 화면인지 확인한 후에만 완료로 보고한다.
- Windows 단축키 자동 입력이 도구의 승인 검토에서 차단되면 우회하지 않는다. 허용된 녹화 UI를 확인하고, 진행할 수 없으면 차단 이유를 설명한 뒤 사용자에게 해당 단축키 입력을 요청한다. 녹화 실패 또는 미시작 상태를 성공으로 기록하지 않는다.

- 각 개발 단계의 테스트 시연은 Windows 화면 녹화 프로그램으로 실제 실행 화면을 녹화하여 MP4 파일로 저장한다. 화면이 없는 자동화 테스트만으로 단계별 시연 영상을 대체하지 않는다.
- 녹화 전 `D:\_Projects\FPS_Dell2g\Docs\_녹화파일\` 폴더를 생성한다. 영상은 날짜별 문서 폴더가 아닌 이 공통 녹화 폴더에 저장한다.
- 영상 파일명은 `<작업날짜> - 작업 제목 - <작업모델명>.mp4` 형식을 사용한다. 작업날짜는 한국 시간(`Asia/Seoul`) 기준 `YY.MM.DD`로 작성하고 작업모델명은 확인 가능한 실제 모델명을 사용한다.
- 예: `Docs/_녹화파일/26.09.14 - 1단계 2인 접속 및 이동 테스트 시연 - GPT-6.mp4`.
- 작업 제목에 단계 번호와 테스트 내용을 포함한다. 같은 테스트를 다시 녹화할 때는 작업 제목에 `재시연 01` 등의 구분을 추가하여 기존 영상을 덮어쓰지 않는다.
- 녹화 후 파일이 실제로 존재하고 재생되는지, 해당 단계의 동작과 테스트 결과가 화면에 담겼는지 확인한다. 사용한 녹화 프로그램, 테스트 조건, 영상 경로와 결과를 해당 작업일의 Markdown 문서에 기록한다.
- 녹화를 수행하지 못했거나 실패한 경우에는 사유와 필요한 조치를 작업 문서에 남기고 시연 영상은 미완료로 표시한다. 실제 녹화 없이 영상 저장을 완료했다고 보고하지 않는다.

## 커밋 및 Pull Request 지침

### 사용자 확정 Git 운영 정책 (2026-09-22)

- 원격 저장소는 `https://github.com/Dell2G96/Badminton.git`, 원격 기준 브랜치는 `main`이다.
- 사용자가 맵과 에셋 업로드를 제외하도록 지정했다. `Content/`, `.umap`, `.uasset`, 이미지·모델·오디오·영상·폰트 등의 바이너리 에셋은 로컬에 보존하고 커밋·push하지 않는다. `git add -f`로 이 정책을 우회하지 않는다.
- 코드, 프로젝트 설정, 실행·검증 스크립트와 텍스트 작업 문서를 버전 관리한다. Git만으로 게임 전체를 복원할 수 있다고 설명하지 않는다. 맵·에셋은 별도 백업이 필요하다.
- 새 기능은 최신 `main`에서 `codex/<작업명>` 브랜치를 만들어 진행한다. 기존 미커밋 작업이 있으면 먼저 소유·범위를 확인하고 함께 섞거나 되돌리지 않는다.
- 관련 변경을 검증하고 기능 단위로 커밋한다. 필요한 검증을 실행하지 못했다면 사유와 미검증 범위를 문서 및 완료 보고에 명시하며 검증 완료로 표시하지 않는다.
- 현재 코드 기준 스냅샷은 신규 빌드·플레이테스트 통과를 의미하지 않는다. 검증된 작업만 `main`에 통합하고, 원격 push는 해당 작업의 사용자 지시 범위에 따른다.
- 기존 로컬 `master`의 과거 이력에는 제거 전 설정 토큰이 있으므로 원격에 push하거나 `main`에 병합하지 않는다. 이 이력은 로컬 보존용이며 토큰 값을 문서·채팅에 기록하지 않는다.

이 저장소에는 Git 메타데이터가 있으므로 작업 시작 시 `git status`와 최근 이력을 확인한다. 기존 사용자 변경과 관련 없는 파일은 수정하거나 되돌리지 않는다. 커밋 제목은 프로젝트의 기존 규칙을 확인하여 간결하게 작성한다. 한 커밋에는 하나의 논리적 변경만 포함한다. PR에는 변경 목적, 구현 요약, 검증 절차, 관련 이슈를 적고, 시각적 또는 게임플레이 변경에는 스크린샷이나 짧은 영상을 첨부한다. 에셋 이동·이름 변경과 생성 파일 포함 여부를 별도로 명시한다. 커밋과 PR 내용도 작업 문서에 기록한다.

## 설정 및 에셋 주의사항

`Default*.ini`에는 개인 경로, 토큰, 비밀 값을 넣지 않는다. 에셋은 Unreal Editor 안에서 이동하거나 이름을 바꾸고 redirector를 정리해 참조 손상을 방지한다.

EOS 로컬 자격 증명은 Git에서 제외된 `LocalSecrets/EOS.credentials.dpapi`에 암호화하여 보관한다. 작업 도구 환경에서 복호화되지 않으면 사용자에게 비밀 값을 채팅으로 요청하지 말고 사용자 직접 실행 런처를 사용한다. 런처는 사용자 접근만 허용한 실행별 폴더의 Engine.ini를 생성하고 게임 종료 후 정리한다. 실행 중 평문 파일이 존재하므로 런처를 강제 종료한 경우 해당 실행의 잔여 파일 정리 여부를 확인한다.
