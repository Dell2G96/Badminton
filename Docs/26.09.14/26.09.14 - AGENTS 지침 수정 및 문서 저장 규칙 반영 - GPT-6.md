# AGENTS 지침 수정 및 문서 저장 규칙 반영

- 작업일: 2026-09-14 (Asia/Seoul)
- 작업 모델: GPT-6
- 대상 저장소: `D:\_Projects\FPS_Dell2g`

## 사용자 요청

이전 배드민턴 GAS 초기 개발계획을 루트 `AGENTS.md`에 반영하고, 모든 내용 및 답변을 `Docs/YY.MM.DD/` 안의 Markdown 파일로 기록하도록 지침을 추가한다. 파일명은 `<작업날짜> - 작업 제목 - <작업 모델명>.md`로 통일한다.

## 진행 답변 기록

1. 기존 `AGENTS.md`를 확인한 뒤, 배드민턴 GAS 개발 방향과 날짜별 문서 저장·파일명 규칙을 반영하겠습니다. 이번 변경 내역도 `Docs\26.09.14` 폴더에 Markdown으로 남기겠습니다.
2. 현재 저장소는 UE 5.7 기반 FPS 프로젝트이며, GAS 의존성은 아직 추가되지 않은 상태입니다. `AGENTS.md`에는 현재 구현과 배드민턴 개발 목표를 구분해 기록하고, 기존 계획서는 새 파일명 규칙에 맞춰 `Docs`에 저장하겠습니다. 문서만 수정한 경우에는 빌드 대신 내용·경로·링크를 검증하도록 지침도 보완하겠습니다.

## 확인 사항과 수행 내용

- 현재 `.uproject`의 EngineAssociation은 `5.7`이며 런타임 모듈은 `FPS_Dell2g`다.
- Build.cs에 EnhancedInput이 존재하며 GAS 모듈 의존성은 아직 없다. `.uproject`에 GameplayAbilities 활성화 항목도 아직 없다.
- 현재 FPS 기반 구조를 설명하면서 신규 배드민턴 개발 목표를 별도로 명시했다.
- 1대1 MVP, GAS 역할, 서버 권한, 셔틀콕 동기화, 개발 순서, 지연·패킷 손실 테스트 기준을 저장소 지침에 반영했다.
- 한국어 기록, 한국 시간 작업일, 날짜 폴더, 파일명, 모델명 확인, 충돌 처리, 진행·최종 답변 기록 규칙을 추가했다.
- 소스·에셋의 원래 경로와 확장자, 루트 AGENTS.md의 위치는 유지하는 것으로 문서 저장 규칙의 범위를 명확히 했다.
- 기존의 Git 메타데이터가 없다는 설명을 실제 `.git` 및 이력 확인 결과에 맞게 수정했다.
- 기존 코딩·설정·에셋 관리 지침을 유지하고 문서 변경 시 적절한 검증 범위를 추가했다.
- 이전 전체 계획서를 새 파일명 규칙으로 저장하고 엔진 기준을 UE 5.7로 구체화했다.

## 변경 파일

- [루트 AGENTS.md](../../AGENTS.md)
- [배드민턴 GAS 초기 개발계획서](<26.09.14 - 배드민턴 GAS 초기 개발계획서 - GPT-6.md>)
- 이 작업 기록 파일

## 검증 결과

프로젝트 설정, Build.cs, 기존 AGENTS.md, Git 상태와 최근 이력을 읽어 현재 상태를 확인했다. 세 문서의 UTF-8 읽기, 날짜·모델명 파일명, 상대 링크 대상 존재 여부를 검증했고 AGENTS.md 전체를 다시 읽어 반영 결과를 확인했다. `git diff --check`는 오류 없이 끝났지만, 기존 `.gitignore`의 `*.md`와 `Docs` 규칙으로 이번 문서는 Git diff에 포함되지 않아 파일 내용을 직접 검증했다. ignore 설정은 변경하지 않았다. 문서만 수정하므로 Unreal 빌드와 실행 테스트는 수행하지 않았다.

기존 미추적 파일 `Plugins/OBSRecording/OBSRecording.uplugin`과 `Plugins/OBSRecording/Source/`는 이번 작업 범위에서 제외하고 보존했다. Git 조회 시 사용자 전역 ignore 파일의 접근 권한 경고가 있었지만 저장소 상태와 이력은 반환되었다.

## 미완료 사항

이번 요청은 지침과 계획서 수정이다. GAS 활성화, 배드민턴 코드 구현, 게임 빌드·실행은 수행하지 않았으며 후속 개발 범위다.

## 최종 답변

[AGENTS.md](D:/_Projects/FPS_Dell2g/AGENTS.md)에 요청 사항을 반영했습니다.

- 배드민턴 GAS 개발 방향, 서버 판정·동기화, 단계별 검증 지침 추가
- 모든 내용과 답변을 `Docs\YY.MM.DD\`에 Markdown으로 기록하도록 규정
- 파일명을 `YY.MM.DD - 작업 제목 - 작업 모델명.md`로 통일

[전체 계획서](<D:/_Projects/FPS_Dell2g/Docs/26.09.14/26.09.14 - 배드민턴 GAS 초기 개발계획서 - GPT-6.md>)와 [변경 기록](<D:/_Projects/FPS_Dell2g/Docs/26.09.14/26.09.14 - AGENTS 지침 수정 및 문서 저장 규칙 반영 - GPT-6.md>)도 저장하고 문서 내용·파일명·링크를 확인했습니다.

현재 `.gitignore`에서 Markdown과 Docs를 제외하고 있어 이번 문서는 Git 추적 대상에는 포함되지 않습니다.
