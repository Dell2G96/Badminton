# 3인칭 마우스 감도와 즉시 타격 입력

- 작업일: 26.09.21 Asia/Seoul. 모델: GPT-6.
- 사용자 요청: 마우스 위치 감도를 공격적으로 높이고 스매시를 제외한 샷은 해당 키를 누르면 바로 타격.
- 시작 시 git status와 최근 이력을 확인했다. 기존 미커밋 변경은 유지한다.
- 이전 기록: [3인칭 전환](<26.09.21 - 3인칭 낙하 타이밍 타격 전환 - GPT-6.md>).

## 구현과 진행

- 3인칭 목표 이동 계수를 .008에서 .032로 변경하여 기존 대비 4배 반응하게 한다. 기존 저장 감도에도 적용되며 -/+ 미세 조절은 유지한다.
- E 클리어, Q 드롭, R 헤어핀, Space 리시브는 키를 누르는 순간 선택 및 타격 RPC를 보낸다. 키 누름 이벤트이므로 누르고 있다고 자동 연타하지 않는다.
- 2 스매시는 선택만 하고 좌클릭으로 타격한다. 서브와 기존 좌클릭 입력도 유지한다.
- 선택과 타격은 같은 컨트롤러의 Reliable RPC 순서로 처리한다. 서버의 거리·하강 타이밍·스태미나·회복·시퀀스 검증을 유지한다.
- HUD를 즉시 타격 키 안내와 감도 4배 안내로 수정했다.
- 기존 5종 타격 프로브가 실제 키 처리 함수를 실행하도록 수정했다. 스매시는 선택만으로 타격하지 않는지 확인 후 클릭한다.
- 실행 중인 이전 게임을 종료하지 않고 BadmintonThirdPersonQuickKeys 별도 패키지를 만든다.

## 변경 파일

- Source/FPS_Dell2g/Badminton/BadmintonPlayerController.cpp
- Source/FPS_Dell2g/Badminton/BadmintonThirdPersonController.cpp
- Source/FPS_Dell2g/Badminton/BadmintonThirdPersonHUD.cpp
- Start-BadmintonAI.cmd (새 패키지로 연결)

## 검증 결과

빌드·PIE·패키지·키 입력 경로의 실제 타격 검사를 완료했다. 상세 결과는 아래와 같다.

## 미완료 사항

사람의 감도 체감과 수동 MP4 녹화는 별도다. 기존 녹화 단축키는 사용자가 직접 조작하는 방식을 유지한다.

### 진행 1

- 목표 이동 감도 4배와 네 종류 즉시 키 타격을 구현했다. 스매시는 2로 선택한 뒤 좌클릭 방식이다.
- 에디터 빌드 성공: `Saved/Logs/Build-ThirdPersonQuickKeys.log`.
- 키 선택 확인 메시지에서 불필요한 좌클릭 안내를 제거하고, HUD에 실제 즉시 타격 키 및 스매시 예외를 표시했다.
- PIE와 별도 패키징을 시작했다. 이번 검사는 일반 키 입력 핸들러를 호출해 서버까지 실제 타격이 연결되는지 확인한다. 물리 판정은 변경하지 않았다.

### 진행 2

- 실제 PIE 검사 통과: `BadmintonDualViewPIE-20260921-092312.log`. 시작 단계의 일반 Condition failed 로그는 이전 실행에도 존재하며, 요청한 PlayModePIE 테스트는 Result={Success}, 프로세스 종료 코드 0으로 완료됐다.
- Windows Development 패키징 성공: `BadmintonPackage-20260921-092311.log`, ExitCode=0.
- 패키지 AI 렌더 검사와 독립 호스트·참가자 입력 검사를 시작했다. 각각 실제 키 처리 함수에서 시작하여 서버의 샷 승인까지 확인한다.

- 새 수동 플레이 창 실행: PID=47508, 로그=D:\_Projects\FPS_Dell2g\Saved\Logs\BadmintonQuickKeys-Manual-20260921-092619.log. 기존 플레이 창은 종료하지 않았다.

## 최종 검증 및 전달

- 에디터 빌드와 실제 PIE, Windows 패키징 성공.
- AI 렌더 패키지 검사 통과: `BadmintonThirdPerson-20260921-092436.log`, contacts=5, mask=62, keyboardShots=1.
- 독립 호스트·참가자 모두 검사 통과: `BadmintonHost/Client-20260921-092439-17935.log`, 양쪽 contacts=5, mask=62, keyboardShots=1. 기본 Legacy 표시 시계, 지연 0ms·손실 0% 설정이다. 이번 입력 수정에서는 지연/손실 조합과 EOS 외부망을 재검사하지 않았다.
- 키 이벤트에 바인딩된 실제 함수를 호출해 RPC/GAS/물리 경로를 확인했다. 물리 키보드 이벤트 주입이나 사람의 마우스 체감 측정을 대신한 검사는 아니다. 사람의 체감 평가는 열린 플레이 창에서 진행한다.
- 패키지 화면의 HIGH CLEAR / E TO HIT 및 하단 즉시 타격 안내, MOUSE TARGET x4 문구를 직접 확인했다.
- `git diff --check` 오류 없음. 신규 단위 테스트는 추가하지 않았고 기존 실제 플레이 프로브를 입력 변경에 맞게 보완했다. 커밋과 PR은 만들지 않았다.
- 새 패키지: `Saved/Packages/BadmintonThirdPersonQuickKeys/Windows/`.
- [Start-BadmintonThirdPerson.cmd](../../Start-BadmintonThirdPerson.cmd)와 [Start-BadmintonAI.cmd](../../Start-BadmintonAI.cmd)는 새 패키지로 연결했다.

![즉시 타격 키 안내](<3인칭 즉시 타격 화면/키 즉시 타격 안내.png>)

사용자 전달: 목표 이동 감도는 기존 대비 4배다. Q 드롭, E 클리어, R 헤어핀, Space 리시브는 키를 누르면 바로 타격한다. 스매시는 2로 선택한 뒤 좌클릭한다. -/+로 감도를 조절할 수 있다. 새 플레이 창을 열었으며 빌드·PIE·AI·2인 입력 검사를 통과했다. 수동 MP4 녹화는 수행하지 않았다.

### 10:44 사용자 요청으로 게임 재실행

- 요청: 게임 실행. 기존 실행 프로세스가 없음을 확인하고 최신 BadmintonThirdPersonQuickKeys 패키지를 창 모드로 실행했다.
- PID=49756, 로그=D:\_Projects\FPS_Dell2g\Saved\Logs\BadmintonQuickKeys-Manual-20260921-104451.log.
- 사용자 안내: 게임 실행 후 Enter로 준비하여 AI 상대 플레이를 시작한다.
- 실행 검증: PID 49756 응답 상태와 게임 로그의 AI 생성·로비 초기화를 확인했다. 소스 변경은 없다.
