# 3인칭과 1인칭 동시 분할 화면

- 요청: 1인칭 조작의 어려움을 보완하도록 한 화면에서 3인칭과 1인칭을 동시에 표시.
- 작업일: 2026-09-21 Asia/Seoul, 모델: GPT-6.
- 시작 상태: 기존 Config, 모듈, 배드민턴 소스와 스크립트 등 다수 미커밋 변경을 확인했으며 유지한다. 최근 커밋 682ce97, ce0cebc.

## 계획과 진행

- 현재 카메라와 입력 구조를 확인한 뒤 한 캐릭터를 두 시점으로 보여주도록 수정한다.
- 왼쪽 높은 3인칭, 오른쪽 라켓 조준용 1인칭. WASD와 마우스는 한 캐릭터에 공유한다.
- 기본 1인칭 렌더링 영역을 오른쪽 절반으로 조정하고 로컬 SceneCapture로 왼쪽 3인칭을 표시한다. 추가 로컬 플레이어나 네트워크 참가자를 만들지 않는다.
- 점수와 타격 안내는 분할 화면 전용 배치로 정리한다. V로 기존 전체 1인칭과 전환할 수 있게 한다.
- 에디터 빌드, 자동화 검사, 실제 렌더링 스크린샷, 새 패키징을 검증한다.

## 변경 파일

- `Config/DefaultEngine.ini`: 배드민턴 전용 GameViewportClient 등록.
- `Source/FPS_Dell2g/Badminton/BadmintonGameViewportClient.h/.cpp`: 엔진 레이아웃 갱신 때 오른쪽 1인칭 영역 유지.
- `Source/FPS_Dell2g/Badminton/BadmintonPlayerController.h/.cpp`: 기본 분할 모드, V 입력, 세로 시야 유지, 종료 정리.
- `Source/FPS_Dell2g/Badminton/BadmintonDualView.cpp`: 로컬 3인칭 캡처, 해상도 적응, 투영, 개발용 렌더/전환 검사.
- `Source/FPS_Dell2g/Badminton/BadmintonCharacter.cpp`: 전역 숨김을 제거하고 OwnerNoSee로 소유자의 1인칭에서만 몸체와 일반 라켓 숨김.
- `Source/FPS_Dell2g/Badminton/BadmintonHUD.h/.cpp`, `BadmintonDualViewHUD.cpp`: 분할 전용 HUD, 셔틀·착지·연습 목표와 샷 방향 표시. 전체 1인칭에는 기존 미니맵 유지.
- `Scripts/Test-BadmintonDualView.ps1`: 실제 렌더와 V 전환 검사.
- `Scripts/Test-BadmintonTiming.ps1`: 분할 HUD 검사 지원 및 SingleView 옵션, 오프스크린 테스트 해상도 고정.
- `Start-BadmintonAI.cmd`: 새 패키지 연결. 이를 호출하는 기존 `Start-BadmintonFPS.cmd`도 새 버전을 실행한다.
- `Start-BadmintonDualView.cmd`: 분할 화면 전용 실행 진입점.
- 본 문서와 같은 폴더의 PNG 3장. 소스 에셋 이동/이름 변경 없음. 생성 패키지/바이너리 커밋 없음. 커밋과 PR은 생성하지 않았다.

## 검증 결과

| 검증 | 결과 |
| --- | --- |
| Development Editor 컴파일 | 성공. 실행 중 사용자 에디터를 유지한 suffix 빌드. 최종 에디터 코드 빌드 `0036`, UAT에서도 빌드 성공 |
| Unreal Automation | 26개 Success, 실패 0. `Saved/Logs/Automation-DualView.log` |
| 에디터 기반 실제 렌더 | 1280×720, 왼쪽 내 캐릭터·오른쪽 조준, V 전환/복구, 단일 로컬 플레이어 확인 |
| 에디터 자동 랠리 | 8회 접촉과 Q/E/R/Space 선택·좌클릭·스매시 타이밍·입력 복구 통과. `BadmintonTiming-20260921-073934.log` |
| Windows Development 패키징 | 성공, UAT ExitCode 0. `Saved/Logs/BadmintonPackage-20260921-073938.log` |
| 패키지 분할 화면 | 1920×1080 실제 viewport 확인, 추가 캡처 640×720, 투영과 전환 PASS. `BadmintonDualView-20260921-074246.log` |
| 작은 창 | OS가 처음 요청 크기를 888×500으로 제한한 실행에서도 분할/전환 PASS. 이후 테스트에 ForceRes를 적용해 1920×1080 검증 |
| 패키지 자동 랠리 | 1280×720, contacts=8, mask=63, directionPreview/aimlock/repeatGuard/racketRay PASS. `BadmintonTiming-20260921-074248.log` |
| 독립 2인 패키지 | 호스트 side=0와 참가자 side=1 각각 localSwings=5, remoteSwings=5, contacts=5. Presentation PASS. 포트 17921, 편도 지연 0ms·손실 0%, 실제 RTT 별도 계측 없음 |
| 스크립트 및 변경 형식 | PowerShell 2개 구문 검사 통과, git diff --check 오류 없음 |

호스트/참가자 로그: `Saved/Logs/BadmintonHost-20260921-074131-17921.log`, `BadmintonClient-20260921-074131-17921.log`.

실행 결과 스크린샷은 직접 열어 확인했다. 자동화 결과는 사람의 조작감 평가나 실제 화면 녹화 완료를 의미하지 않는다.

## 미완료 사항

- 사용자 수동 Windows + Alt + R 녹화와 실제 조작감 플레이테스트는 아직 수행하지 않았다. 자동화/스크린샷은 MP4 시연을 대체하지 않는다.

### 진행 1

- 로컬 플레이어의 오른쪽 절반 1인칭 렌더링과 왼쪽 3인칭 SceneCapture를 연결했다.
- 3인칭은 코트 방향을 유지하며 캐릭터를 뒤/위에서 따라간다. 1인칭은 세로 시야 유지로 좁은 화면의 상하 시야 손실을 줄인다.
- 추가 시점 해상도는 높이 720px로 제한하며 V 단일 시점 전환 시 캡처를 중지한다.

### 진행 2

- 분할 HUD에 점수, 체력, 샷 선택, 타이밍 게이지, 셔틀/착지 표시를 배치했다. V는 전체 1인칭과 분할 시점을 전환한다.
- 첫 일반 에디터 빌드는 기존 실행 에디터가 UnrealEditor-FPS_Dell2g-0030.dll을 사용 중이라 실패했다. 사용자 에디터는 종료하지 않고 0031 suffix 빌드로 검증한다.
- 별도 렌더링 검사에 로컬 플레이어 수, 오른쪽 조준 투영, 왼쪽 캐릭터 투영, V 키 전환과 복구 검사를 추가했다.

### 진행 3

- 기존 자동화 26개 모두 Success. 로그: Saved/Logs/Automation-DualView.log.
- 실제 렌더 검사에서 엔진 LayoutPlayers가 Origin/Size를 매 프레임 초기화함을 확인했다. 전용 UBadmintonGameViewportClient::LayoutPlayers로 배드민턴 1인 로컬 플레이일 때만 오른쪽 1인칭 영역을 유지하도록 수정했다.
- 전용 ViewportClient는 배드민턴 컨트롤러와 로컬 플레이어 1명 조건에서만 분할 레이아웃을 지정한다. 여러 로컬 플레이어 구성은 이번 요청과 검증 범위에 포함하지 않는다.

### 진행 4

- 분할/단일 화면 전환과 복구, 양쪽 투영 및 로컬 플레이어 1명 검사가 통과했다.
- 스크린샷 검사에서 로컬 PrototypeBody가 전역 SetVisibility(false)로 숨겨져 있음을 확인해 OwnerNoSee 방식만 유지했다. 왼쪽에 내 캐릭터가 나타나고 오른쪽에서는 정상적으로 숨겨진다.
- 단일 화면 캡처 전에 복구 타이머가 같은 프레임 실행되던 검사 문제를 수정하고 두 결과 이미지를 확인했다.
- 분할 화면에서도 샷 방향 화살표와 연습 목표 코트 표시를 유지한다. 단일 화면에서는 기존 미니맵을 사용할 수 있다.

### 진행 5

- 에디터 분할 화면 자동 랠리: contacts=8, mask=63, 샷 선택 후 기본 리시브 복구, 스매시 타이밍, aimlock, repeatGuard, racketRay 모두 통과했다.
- 분할 HUD의 셔틀·착지·방향 화살표 로그와 실제 BadmintonTiming/BadmintonContactReady 스크린샷을 확인했다.
- Windows Development 새 패키지 BadmintonDualView를 생성 중이다. 기존 패키지는 보존한다.

### 진행 6 및 최종 결과

- 새 Windows 패키지와 2인 타격 연출 검사를 통과했고, 기존 AI/FPS 실행 메뉴를 새 버전에 연결했다.
- 최종 패키지에서 1920×1080 분할·전환 검사와 1280×720 자동 랠리 8회까지 통과했다.
- **왼쪽 3인칭 / 오른쪽 1인칭**을 기본값으로 적용했다. 왼쪽은 캐릭터를 높은 후방에서 따라가고 코트 방향을 유지하며, 오른쪽은 기존 마우스 라켓 조준을 보여준다.
- 입력은 기존 WASD·마우스로 하나의 캐릭터를 조작한다. **V**로 전체 1인칭과 분할 화면을 전환한다.
- 실행: 루트 `Start-BadmintonDualView.cmd`. 기존 `Start-BadmintonAI.cmd`와 `Start-BadmintonFPS.cmd`도 새 버전을 실행한다.
- 패키지: `Saved/Packages/BadmintonDualView/Windows/FPS_Dell2g/Binaries/Win64/FPS_Dell2g.exe`.
- 분할 시 추가 렌더 비용이 발생하며 추가 화면 해상도는 높이 720px로 제한했다. 목표 FPS와 GPU 비용은 별도 성능 측정하지 않았다.

## 남은 확인과 제한

- 기존 사용자 에디터를 강제로 종료하거나 PIE 세션을 조작하지 않았다. 새 ViewportClient 설정이 필요한 에디터 PIE는 에디터 재시작 후 별도 확인이 필요하다. 이번 실제 화면 검증은 별도 `-game` 프로세스 및 패키지에서 수행했다.
- 사용자 수동 Windows + Alt + R 녹화와 MP4 시연은 미수행이다. 사용자 직접 녹화 시작/종료에 맞춘 후속 확인이 필요하다.
- 사람의 실제 조작감 평가, EOS 인증·외부망, RTT/손실 전체 조합의 재검증은 이번 작업에서 수행하지 않았다.

## 실제 화면

![분할 화면에서 타격 준비](<26.09.21 - 분할 화면 타격 준비.png>)

![패키지 1920x1080 분할 화면](<26.09.21 - 패키지 분할 화면 1920x1080.png>)

![V로 전체 1인칭 전환](<26.09.21 - 전체 1인칭 전환.png>)

## 최종 사용자 안내

왼쪽 3인칭과 오른쪽 1인칭을 동시에 표시하도록 수정했다. 기존 조작은 유지하며 V로 전체 1인칭과 전환할 수 있다. 새 실행 파일은 Start-BadmintonDualView.cmd다. 빌드·패키징, 자동화 26개, 실제 분할 렌더링과 2인 타격 연출 검사를 통과했다. 사람의 플레이테스트와 MP4 녹화는 아직 수행하지 않았다.

## 작업 재개 — 08:34 KST

- 사용자 요청: 작업 재개.
- 현재 기록과 프로세스를 확인했다. 실행 중인 Unreal Editor/게임은 없으며 기존 분할 화면 패키지와 소스가 유지되어 있다.
- 남은 PIE 확인을 독립 에디터 자동화로 진행하고, 이후 사용자가 직접 플레이하고 지정 단축키로 녹화할 수 있게 준비한다.
- 기존 패키지·자동 랠리·2인 타격 검증 결과를 반복 수행하지 않고 남은 PIE 검증에 집중한다.

### 재개 진행 1

- Source/FPS_Dell2g/Tests/BadmintonDualViewPIETests.cpp를 추가했다. 실제 PIE 시작, 배드민턴 ViewportClient 활성화, 로컬 플레이어 1명, 양쪽 투영, 단일 시점 전환과 복구를 프레임 간 검사한다.
- 에디터 빌드에만 UnrealEd 의존성을 추가했다. NonNullRHI 플래그로 렌더링 없는 검사에서는 PIE 테스트를 제외한다.
- 일반 빌드 진행 중 테스트 include 경로 누락을 확인하여 기존 저장소의 FPS_Dell2g/ 접두사 방식으로 수정했다.

### 재개 진행 2

- 일반 Development Editor 빌드가 성공했다. 현재 실행 중인 에디터가 없으므로 suffix 없이 정상 모듈로 갱신했다.
- 새 독립 에디터 PID 44860에서 렌더링을 활성화한 PIE 자동화 검사를 시작했다. 로그: Saved/Logs/Automation-DualViewPIE.log.
- 재현용 Scripts/Test-BadmintonDualViewPIE.ps1를 추가했다. 검사 프로세스만 관리하며 기존 사용자 에디터에는 접근하지 않는다.
- 녹화는 AGENTS.md에 명시된 사용자 직접 Windows + Alt + R 시작/종료 방식으로만 준비한다. 기존 다른 녹화 스크립트는 실행하지 않았다.

### 재개 완료 — PIE 검증 및 직접 플레이 준비

- 일반 Development Editor 빌드 성공: Saved/Logs/Build-DualViewPIE.log.
- FPS_Dell2g.Badminton.Camera.DualViewPIE 테스트 **Success**, 프로세스 정상 종료 코드 0.
- 실제 PIE world, 배드민턴 ViewportClient, 로컬 플레이어 수 1명, 좌/우 투영, 분할→단일→분할 전환 및 조준 보존을 확인했다.
- Saved/Screenshots/BadmintonDualViewPIE.png를 직접 열어 에디터 내부의 동시 화면과 로컬 캐릭터 표시를 확인했다. 기록 이미지: `26.09.21 - PIE 분할 화면 검증.png`.
- GameplayCueNotifyPaths 미지정에 대한 기존 경고 1건이 있으나 테스트 실패는 없다. 이번 카메라 작업과 무관하여 설정 변경하지 않았다.
- 추가 변경 파일: `Source/FPS_Dell2g/FPS_Dell2g.Build.cs`(에디터 전용 UnrealEd), `Source/FPS_Dell2g/Tests/BadmintonDualViewPIETests.cpp`, `Scripts/Test-BadmintonDualViewPIE.ps1`, 본 문서 및 PIE PNG.
- PowerShell 구문 검사와 git diff --check 통과. 신규 검사는 편집기 전용이므로 기존 Windows 패키지는 변경하지 않았다. 기존 26개 자동화 통과에 이어 이번 PIE 검사 1개가 추가 통과했으며, 27개 전체를 일괄 재실행한 것은 아니다.
- PIE 미검증 항목은 이제 해소되었다. 위의 이전 단계 기록은 당시 상태로 보존한다.

![실제 에디터 PIE 분할 화면](<26.09.21 - PIE 분할 화면 검증.png>)

### 수동 플레이 및 녹화 순서

1. 게임 창을 클릭하고 Enter로 AI 경기를 시작한다.
2. WASD 이동, 마우스 조준, 좌클릭 타격으로 두 화면을 함께 확인한다.
3. V로 전체 1인칭 전환과 분할 화면 복귀를 확인한다.
4. 시연 녹화 시 사용자가 Windows + Alt + R을 눌러 시작하고, 플레이 후 같은 키로 종료한다.
5. 저장된 영상 파일을 확인한 뒤 Docs/_녹화파일에 날짜·단계·제목을 갖춘 이름으로 복사하고 디코딩 및 내용 검증을 수행한다. 아직 녹화 파일은 생성하거나 완료 처리하지 않았다.

최종 안내: 작업을 재개해 남은 PIE 검증까지 완료했다. 수동 플레이용 분할 화면 게임을 실행했다. Enter로 시작하고 V로 시점을 전환할 수 있다. 사람의 실제 조작감 평가와 사용자 수동 녹화는 여전히 미완료다.

- 수동 실행 프로세스 PID 44140. 실행 로그: Saved/Logs/BadmintonDualView-Manual-20260921-0841.log. 자동 타격 테스트 인수 없이 실행했으며 녹화 단축키를 입력하지 않았다.
